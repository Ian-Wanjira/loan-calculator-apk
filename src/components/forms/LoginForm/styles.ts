import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  form: {
    gap: 8,
    padding: 16,
  },
  signUpContainer: {
    textAlign: 'center',
    marginTop: 16,
    fontSize: 14,
    color: '#333',
  },
  signUpText: {
    color: '#007BFF',
    textDecorationLine: 'underline',
  },
});

export {styles};
