import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  form: {
    gap: 8,
    padding: 16,
  },
  loanTermsContainer: {
    flexDirection: 'row',
    gap: 10,
  },
  loanTermItem: {
    flex: 1,
  },
});

export {styles};
